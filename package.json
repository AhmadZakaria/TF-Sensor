{
  "name": "seminar-webapps-wot",
  "version": "0.1.0",
  "description": "Bonn-IT code examples for the new lessons scripts",
  "repository": {
    "type": "git",
    "url": "https://github.com/webcc/seminar-webapps-wot.git"
  },
  "author": {
    "name": "Web Compliance Center",
    "email": "webcc@fit.fraunhofer.de"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "express": "4.14.0",
    "ws": "^1.1.1"
  },
  "devDependencies": {
    "mocha": "^3.2.0"
  },
  "engines": {
    "node": ">=6.9.1"
  },
  "scripts": {
    "postinstall": "cd _sensors-project/generic-sensor-api && npm install && cd ../dummy-sensor && npm install && cd ../tf-sensor && npm install && npm install && cd ../generic-sensor-api/",
    "start": "cd ./_sensors-project/sensor-server/ &&  node index"
  }
}
